### Фаза 4 — Microcycle view (feat/microcycle-view)
**Цель:** список сессий внутри микроцикла + создание сессии

- `features/create-session` — bottom sheet (категория, дата, длительность)
- `widgets/session-list` — строки сессий
- `views/microcycle` — компоновка
- `app/microcycles/[id]/page.tsx`

**Ветка:** `feat/microcycle-view`
